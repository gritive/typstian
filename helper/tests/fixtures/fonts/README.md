# Font fixture

`colr_1.ttf.base64` is the base64-encoded `tests/fonts/colr_1.ttf` fixture from
`ttf-parser` 0.25.1. It is used only to verify configured font-directory scanning.
The upstream project is dual-licensed under MIT or Apache-2.0.
